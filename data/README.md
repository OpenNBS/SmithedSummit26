# Project data

`generated/` means produced by tooling, not necessarily uncommitted. The manifest
and thumbnails are required by credential-free CI builds and should remain tracked.
Only reproducible or restricted files such as downloaded `*.nbs` songs are ignored.
