# Beet config for used for live reload (development). Writes file directly to
# filesystem instead of zipping.

extend: beet.yml

data_pack:
  zipped: false

resource_pack:
  zipped: false

output: dist

meta:
  debug: true
  song_manifest_path: "manifest.json"
